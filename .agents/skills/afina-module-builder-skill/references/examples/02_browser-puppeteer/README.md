# 02 browser-puppeteer

Minimal browser Script Module for Afina.

Shows:

- Afina `wsEndpoint` connection
- canonical `utils.js`
- `getCurrentPage()` instead of `browser.pages()[0]`
- full-load wait, element wait, and random post-action wait
- safe `saveTo` handling through `readKey(...)`
